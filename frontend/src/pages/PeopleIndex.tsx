import type { Person } from "../api/types";

// Person type imported to exercise the API types in the build.
export type { Person };

function PeopleIndex() {
  return <h1>People</h1>;
}

export default PeopleIndex;
