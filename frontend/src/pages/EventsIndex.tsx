import type { Event } from "../api/types";

// Event type imported to exercise the API types in the build.
export type { Event };

function EventsIndex() {
  return <h1>Events</h1>;
}

export default EventsIndex;
